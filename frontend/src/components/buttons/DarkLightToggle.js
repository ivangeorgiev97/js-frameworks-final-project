import { Button } from 'reactstrap';

const DarkLightToggle = (props) => {
    return (
        <Button id="dark-cards" type="button" color="warning" className="my-2">Тъмни елементи</Button>
    );
}

export default DarkLightToggle;
